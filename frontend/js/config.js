// ---------------------------------------------------------------------
// InboxIQ frontend config
// ---------------------------------------------------------------------
// Point this at your deployed backend's URL (no trailing slash).
// Leave it as an empty string only if the frontend is served from the
// exact same origin as the backend (rare once they're deployed separately).
//
// Local development example:
//   window.API_BASE_URL = "http://localhost:5000";
//
// Production example (after deploying the backend on Render):
//   window.API_BASE_URL = "https://inboxiq-backend.onrender.com";
// ---------------------------------------------------------------------
window.API_BASE_URL = "https://e-mail-backend-7zaq.onrender.com";
